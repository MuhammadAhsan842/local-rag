requests>=2.31
PyYAML>=6.0
# Optional, only for the apply-assist step (Browser Use drives a real browser
# with your local Ollama model):
#   pip install browser-use langchain-ollama
#   playwright install chromium
# Optional, only for the local dashboard:
#   pip install gradio
# Optional, for CV import (PDF/DOCX) and tailored-CV .docx output:
#   pip install pdfminer.six python-docx
# Dev only, to run the test suite:
#   pip install pytest
